// Package cfg defines the cfg schema contract — the structural grammar of a
// service's declarative config. The grammar is derived from the tool function
// vocabulary, so it follows the tool catalogs (Phase 1) and is locked in Phase 2.
//
// "Config is the program": the cfg format is the language the interpreter reads.
package cfg
