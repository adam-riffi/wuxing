// Package sdk defines the SDK / script contract — how a service's script is
// entered, how it reports its fact, and how it emits to the bus. The contract is
// the universal anatomy: every tool and service is cfg + scripts against it, and
// wuxing dogfoods its own SDK. Locked in Phase 2.
package sdk
