# Normalize line endings: LF in the repo, on every platform. Go tooling (gofmt,
# gofumpt) and the linux/mac CI runners expect LF; this keeps Windows checkouts
# from introducing CRLF diffs.
* text=auto eol=lf

# Explicitly binary (never normalize).
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.pdf binary
